node_modules/
.next/
out/
next-env.d.ts
lib/search.built.mjs
# The 4MB search payload is published by the daily pipeline and fetched at runtime,
# so it is never committed. The build-time exports ARE committed - Vercel builds
# Node and cannot run the Python that produces them.
public/data/
tsconfig.tsbuildinfo
