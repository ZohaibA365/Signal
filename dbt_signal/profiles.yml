signal:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5433') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'signal') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'signal') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'signal') }}"
      schema: public
      threads: 4
    # Hosted Neon warehouse behind the public dashboard. Uses the direct
    # (non-pooler) endpoint: dbt creates and drops tables, and PgBouncer in
    # transaction mode does not pass through everything DDL needs.
    prod:
      type: postgres
      host: "ep-wispy-dew-ay07k67r.c-5.us-east-2.aws.neon.tech"
      port: 5432
      # No defaults here on purpose: profiles.yml is committed, so a default
      # would put the password in git history. These must come from .env.
      user: "{{ env_var('NEON_USER') }}"
      password: "{{ env_var('NEON_PASSWORD') }}"
      dbname: "neondb"
      schema: public
      sslmode: require
      threads: 4

    # Snowflake as an ADDITIONAL target, not a replacement. Neon stays the
    # live warehouse behind the dashboard; this builds the same models into
    # Snowflake so the project is not welded to one engine.
    #
    # Needs SNOWFLAKE_ACCOUNT, SNOWFLAKE_USER and SNOWFLAKE_PASSWORD in .env.
    # Build with:  dbt build --target snowflake
    snowflake:
      type: snowflake
      account: "{{ env_var('SNOWFLAKE_ACCOUNT') }}"
      user: "{{ env_var('SNOWFLAKE_USER') }}"
      # Key-pair, not a password. The account enforces MFA, which is a property
      # of interactive login: a password-presenting driver is rejected with
      # "Multi-factor authentication is required for this account" and no retry
      # changes that. The private key is gitignored and never leaves this
      # machine; only its public half is registered on the Snowflake user.
      private_key_path: "{{ env_var('SNOWFLAKE_PRIVATE_KEY_PATH', '../.snowflake_key.p8') }}"
      # SIGNAL_LOADER, not ACCOUNTADMIN. dbt creates and drops tables in one
      # schema; ACCOUNTADMIN can drop the account's billing configuration.
      role: "{{ env_var('SNOWFLAKE_ROLE', 'SIGNAL_LOADER') }}"
      database: "{{ env_var('SNOWFLAKE_DATABASE', 'SIGNAL_DB') }}"
      warehouse: "{{ env_var('SNOWFLAKE_WAREHOUSE', 'COMPUTE_WH') }}"
      schema: "{{ env_var('SNOWFLAKE_SCHEMA', 'PUBLIC') }}"
      threads: 8
      client_session_keep_alive: false

    # Databricks as a THIRD target, and for a different reason from Snowflake.
    #
    # Snowflake catches regex anchoring. Databricks catches type coercion:
    # subtracting two dates yields an INTERVAL on Spark rather than an integer, so
    # days_since_posted silently changes type and a comparison against 60
    # misbehaves - compiles fine, wrong answer, which is the same shape as the
    # regex bug and a class Snowflake cannot see, because Spark's rlike is
    # unanchored exactly like Postgres.
    #
    # Needs DATABRICKS_HOST, DATABRICKS_TOKEN and the SQL warehouse's http_path.
    # Source tables are mirrored by storage/load_to_databricks.py.
    # Build with:  dbt build --target databricks
    databricks:
      type: databricks
      # The host without its scheme, which is how this adapter wants it while
      # DATABRICKS_HOST carries https:// for every other caller.
      host: "{{ env_var('DATABRICKS_HOSTNAME') }}"
      http_path: "{{ env_var('DATABRICKS_HTTP_PATH') }}"
      token: "{{ env_var('DATABRICKS_TOKEN') }}"
      catalog: "{{ env_var('DATABRICKS_CATALOG', 'workspace') }}"
      schema: "{{ env_var('DATABRICKS_SCHEMA', 'signal') }}"
      # One. Free Edition allows five concurrent tasks on one 2X-Small warehouse,
      # and a parity build is not in a hurry.
      threads: 1
